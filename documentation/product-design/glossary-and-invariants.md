# Термины и инварианты

Словарь пакета и инварианты, которые держат все модули сразу. Термин определяется
здесь один раз; остальные документы на него ссылаются.

Границы: как реестр устроен и ведёт себя в коде —
[`../architecture/06-registry-and-persistence.md`](../architecture/06-registry-and-persistence.md);
state-DTO — [`rest-api/contracts/state-contracts.md`](rest-api/contracts/state-contracts.md);
общие типы состояния — [`bus-contracts/shared-state-contracts.md`](bus-contracts/shared-state-contracts.md);
что построено — [`status.md`](status.md).

## Термины

- **Controller** — один контроллер на единственной плате Waveshare ESP32-P4-ETH
  ([`../architecture/01-overview.md`](../architecture/01-overview.md)) с одним или
  несколькими DALI-адаптерами.
- **Adapter** — физическая шина DALI со своим транспортом; адресное пространство
  адаптера — 64 коротких адреса gear, 64 виртуальные лампы, 16 групп, 16 сцен и
  отдельно 64 адреса устройств ввода.
- **`adapter_id`** — логический индекс адаптера (`u8`, `0..N-1`) в реестре и
  REST-путях. В payload'ах шины он называется `registry_adapter_id` и **не** равен
  `target_adapter_id` конверта — идентификатору экземпляра шины.
- **Physical device** — control gear с коротким адресом на адаптере.
- **Virtual lamp** — стабильная пользовательская сущность UI и Home Assistant
  (`virtual_lamp_id`, `0..63`); привязана к физическому устройству или нет.
  Своих заявлений типа и цвета не имеет — наследует их от устройства.
- **Binding** — привязка лампы к короткому адресу; разрешается исполнителем в момент
  исполнения команды.
- **Group / Scene** — группа `0..15` и сцена `0..15` одного адаптера.
- **Desired** — конфигурация группы или сцены, заданная через API и ещё, возможно, не
  записанная в гир. **Applied** — то, что гир держит по последнему readback'у.
- **Control device / input device** — управляющее устройство IEC 62386-103 (кнопка,
  датчик); **instance** — его вход определённого типа. Адреса устройств ввода — своё
  пространство, не пересекающееся с адресами gear.
- **Semantic DALI command** — типизированная команда продуктового пути без ручной
  сборки опкода или кадра. **Diagnostic raw command** — явное исключение: сырой
  16-битный кадр без продуктовой семантики.
- **Operation** — долгая работа, на которую маршрут ответил `202` и `operation_id`.
- **Role** — `active` или `standby` контроллера на общем сегменте; решается проводом.
- **Slice** — именованная единица персистентной конфигурации (`SliceKey`) со своей
  версией формата.

## Инварианты

- **Один писатель.** Registry worker — единственный владелец изменяемого состояния
  (документ правил — у rules worker); HTTP, мост Home Assistant, WebSocket, HCL, правила
  и будущие кластер и adapter proxy реестр напрямую не мутируют. Все читают через
  read-порты без методов записи.
- **Один владелец провода.** Только `DaliWorker` вызывает транспорт DALI; приоритет,
  прерывание, транзакции и гейты живут там.
- **Семантический продуктовый путь** — продукт не собирает опкоды и кадры
  ([09](../architecture/09-dali-protocol-rules.md#product-path-and-diagnostic-path)).
- **Runtime — только проекцией.** Volatile-состояние лампы меняется только
  `RegistryRuntimeUpdateCommand`, а его единственный издатель — проектор state-fanout.
  Производитель пишет ровно то, что наблюдал: отсутствие поля значит «не наблюдал».
- **Applied — только из доказательств.** Applied членство и строки сцен выводятся из
  readback'ов программирования и чтения; массовое применение разворачивает
  оркестратор по снапшоту реестра (`ADR-007`), а шлюз держит только неавторитетный
  быстрый путь пустого diff.
- **Более поздняя команда на ту же цель вытесняет раннюю**; вытесненная, от которой в
  выжившую не перешло ни одного поля, получает `409 superseded`
  ([03](../architecture/03-bus-and-backpressure.md) §Coalescing).
- **Канон shared state и payload'ов — Rust-типы** `dali2rust-contracts::msg`; размер
  кадра считается по `postcard`. JSON существует только на границах REST, WebSocket и
  MQTT и покрыт контрактными тестами.
- **`wire_address`** встречается только в низкоуровневом контракте провода; продуктовый
  API говорит `adapter_id`, `virtual_lamp_id`, `group_id`, `scene_id`, `short_address`.
- **Коммиссионинг** на адаптере взаимоисключающий
  ([`commissioning.md`](rest-api/resources/commissioning.md) §Исключение на адаптер).
- **Цвет** в публичных DTO и событиях — кельвины и sRGB-каналы; миреды и линейные dim
  level существуют только на проводе.
- **Роль решает провод**: состояние сети роль контроллера не двигает.

Что персистится, а что нет — [`bus-contracts/snapshots.md`](bus-contracts/snapshots.md).
