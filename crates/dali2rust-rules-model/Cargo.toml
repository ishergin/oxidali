[package]
name = "dali2rust-rules-model"
description = "Typed rule graph, limits and compiler traits for the rules engine (I10)"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
