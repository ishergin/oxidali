# Стратегия HIL-тестирования dali2rust

Что стенд проверяет в прошивке и чему из его показаний можно верить: оракулы и пределы
доверия, правила работы на боевой установке, карта покрытия, watchlist и долг стенда.
Документ русскоязычный по решению мейнтейнера; код тулкита — на английском.

**Границы.** Как настраивать, запускать и прошивать — runbook [README.md](README.md).
Архитектура — [`documentation/architecture/`](../../documentation/architecture/README.md), слои
тестов — [05-testing-and-bdd.md](../../documentation/architecture/05-testing-and-bdd.md). Дефекты
продукта и приёмки исправлений — [known-issues.md](../../documentation/product-design/known-issues.md);
номера `ISSUE-NN` — только в [реестре](../../documentation/product-design/issue-ids-registry.md).
Отступления от IEC 62386 — [iec62386-conformance-gaps.md](../../documentation/reference/iec62386-conformance-gaps.md).

## 1. Назначение и границы

HIL проверяет прошивку на настоящем проводе DALI, рядом с чужими мастерами и настоящими
гирами. Это слой поверх unit-, crate-тестов и BDD и не merge-гейт; он доказывает то, чего хост
не моделирует:

- тайминги провода и ответы реальных балластов: молчание, конкуренция, нарушающие кадры;
- два контроллера на одном проводе: арбитраж, репликация, переключение роли;
- HTTP- и WebSocket-стек устройства, а не хостовый парсер;
- прерывание PHY, флеш и задачи на двух ядрах; глубину стеков и пол внутренней SRAM;
- персистентность на NOR-флеше, в том числе новую раскладку слайсов поверх старой (хост
  смещений не воспроизводит; фолбэк решается по содержимому, а не по факту чтения);
- диалект внешнего мира: брокер MQTT, Home Assistant, чужой мастер.

Формы HTTP и негативы — канон BDD. `tests/hil/*.rs` в корне — другой инструмент (петля PHY).
Показания железа, нужные хосту, замораживаются в `corpus/` (`hil corpus`) и закрепляются
хост-тестами; корпус локальный, в git только файлы, которые читают тесты (`.gitignore`).

## 2. Инвентарь стенда

Отдельного стенда нет: тесты работают на боевой установке.

| Компонент | Что важно |
| --- | --- |
| Основной контроллер (DUT) | Waveshare ESP32-P4-ETH + Pico-DALI2, роль active, адрес по умолчанию — `hil/config.py`. Прошивка — только `hil flash`, сборкой стенда (`bench.env`: провод делят чужие мастера). |
| Пир (standby) | Второй ESP32-P4-ETH с той же прошивкой ([ADR-018](../../documentation/architecture/decisions/ADR-018-controller-redundancy.md)); `hil --peer`; прошивка — `hil --peer flash` или OTA. Его 24-битный зонд арбитража — постоянный фон провода. |
| Сегмент DALI | Боевой свет: SA01 и адреса от 4. Стендовых светильников SA00/SA02/SA03 на проводе нет. Панель Part 103: адрес 0, четыре инстанса, схема событий 2, индикация Part 332. |
| Wiren Board (WB) | Хаб установки: WB-MDALI-3 под `wb-mqtt-dali` (сниффер и чужой мастер — эмулятор Lunatone, WebSocket :8080), mosquitto, Home Assistant, USB и питание обеих плат (перезагрузка WB перезапускает обе). `wb-mqtt-dali` сам опрашивает статус гиров примерно раз в 120 с, и это не выключается: тихой шины не бывает. Под штормом декодирования сервис клинит (журнал молчит) — `systemctl restart wb-mqtt-dali`; порт 8080 поднимается после этого до десятков минут, `foreign`-тесты пока скипаются. |
| Камера | Фотометрическая UVC-камера на хосте через frame server. Калиброванных светильников на проводе нет, поэтому прогоны идут с `--no-camera`. |
| Хост | macOS, `tools/hil/.venv`; до WB — LAN установки. |
| Эмулятор gear, арбитр | Нет: платы нет, эмулятор на P4 не портирован ([`tools/dali-gear-sim`](../dali-gear-sim/README.md)). Тесты с `gear_sim` скипаются. |

## 3. Оракулы и пределы доверия

Зелёный тест доказывает ровно то, что видит его оракул.

### 3.1 Сниффер WB

- Кольцо из четырёх слотов MQTT с кадрами, которые инициировал не WB (наши и ответы гиров),
  плюс декодированный топик монитора. Раскладку слова разбирает `hil/sniffer.py`; счётчик
  кадров даёт порядок и дыры.
- Кольцо роняет бёрсты (например, кадры приоритета 1 вплотную к доказательствам DTR). «Кадр
  был» — сильное утверждение при шаге стимулов от 0,7 с (`paced`); «кадра не было»
  (`expect_quiet`) — растяжка, верная только при нуле дыр; скан и коммишенинг считаются по
  дырам, а где кольцо не успевает, оракул — эффект на гире (§3.3).
- `expect_program_witness` принимает readback вместо программирующего кадра и считает это в
  `witness_fallbacks` (бюджет 0). `expect_frame` один раз переотправляет стимул; повтор виден
  только в леджере.
- Собственных передач WB кольцо не несёт: инжекция видна в топике монитора
  (`Window.monitor_lines()`) и в счётчиках DUT. Доставка по слотам переставляет кадры —
  порядок проверять по счётчику WB.
- Потерянный и недекодируемый кадр для сниффера одно и то же: наш сигнал он не судит. Эталон —
  собственный опрос WB, доля ответов на запросы, которые отвечают всегда; сравнивать с ним наш
  путь — только при равной избыточности (повторы, переотправка special-кадров).

### 3.2 Чужой мастер WB

- `hil/foreign.py` кладёт на провод 16- и 24-битные кадры: проекция чужих команд в реестр,
  события Part 103, триггеры правил. Инжекция доказывает всё выше провода и ничего о прошивке
  панели; настоящие нажатия записаны в `corpus/panel/`.
- Эхо WebSocket — WB, согласный сам с собой. Шлюз WB-MDALI может отказать в передаче
  (`no response from gateway`, `internal gateway error`) и молча обрезает длинный пакет:
  свидетель сверяет наш кадр по содержимому монитора, длинные последовательности идут по
  кадру на вызов, а непереданная инъекция — отказ прибора, не вердикт о прошивке.
- Событие Part 103 с адреса настоящей панели инжектируется только в инстанс, на который не
  заведено правил, иначе оно выстрелит боевыми правилами; фикстура находит такой инстанс
  в документе правил.

### 3.3 Гир как оракул

- **Селектор 208.** `QUERY COLOUR VALUE` с DTR0 = 208: после активации temporary — MASK
  (IEC 62386-209 §9.12.5). `0xFF` — цвет активирован; тип цвета (`0x10` xy, `0x20` Tc, `0x80`
  RGBWAF) — застажен и ждёт; молчание — нет показания. Работает там, где камеры нет: группы,
  broadcast, xy (`api.colour_is_activated`, `None` на молчание).
- **Эффект вместо провода:** длительность фейда по биту 4 `QUERY STATUS` против min/max
  Table 4; принятый, а не запрошенный байт; `QUERY GROUPS` после apply.

### 3.4 Само-взгляд контроллера (API)

- Реестр — само-взгляд DUT: он обязан сходиться с проводом и гиром, расхождение — находка.
  `state.value_source` называет источник последнего значения: после чужой записи новое
  значение может значить «видели кадр», а не «гир применил».
- Диагностический путь `/api/v1/dali/*` в реестр не отображается, свои кадры в
  сниффер-транслятор не попадают: после такого теста реестр устарел, а расхождение с UI —
  не потерянное WebSocket-событие. Чужой `DAPC` в реестр попадает.
- `POST /api/v1/dali/raw` при молчании отвечает `backward_frame: 0` и `success: false`:
  сначала `success`. Молчание — законное DALI «нет», поэтому потерю меряют как в §3.1.
- Операция — не провод: провод виден покадрово в serial (`DALI PHY TX` с миллисекундами DUT,
  `scan_wire_timing.py`). `404` на операцию `wait_op` читает по контракту
  ([operations.md](../../documentation/product-design/rest-api/resources/operations.md)).
- Счётчики читаются дельтой от начала теста.

### 3.5 Serial и пир как приёмник

- Порты обеих плат и логи — [README](README.md#serial-ports). Время — только по штампам
  прошивки `I (ms)`: хост штампует сетевое чтение пачкой.
- Serial даёт маркер загрузки (`HTTP server listening` в окне теста — перезагрузка, тест
  падает через `dut_continuity`), перепись стеков и лестницу кучи для `HIL validity`, кадры TX,
  неразобранные захваты (есть только здесь), поздние тики, удержания линии, приём за Table 20.
- Пир не делит с DUT ни часов, ни прерывания: его `owned` засчитывает ответ, начавшийся внутри
  гейтов Table 20, поэтому `answered Δ == owned Δ` при `late Δ == 0` — замер позиции нашего
  ответа (`HIL-RED-07/08`).

### 3.6 Камера

- Годится только фотометрическая камера; эффект-проба калибровки — жёсткий гейт. Frame server
  один: два пишут в один почтовый ящик и портят кадры. Поток обязан двигаться от команды
  экспозиции через `uvc-util`, иначе снимается другая камера (через хаб на Full Speed камера
  отдаёт только 160x120, и открывается встроенная); это проверяет `hil preflight`.
- Верим только крайним цветам (один канал, полная шкала, чёрный) и порядку CCT с
  лампой-якорем; средние тона судит глаз против свотча.
- Клип — слепота, а не число: `rb_ratio` ровно 1.000 неотличим от «цвет не сдвинулся». Слепой
  замер — `ColourBlindError`, мёртвый прибор (§3.7).
- Базлайн `CameraOracle.fresh_baseline()` (и `hil lamps` без `--no-baseline`) гасит лампы:
  замер после него меряет тёмный риг, а на боевом проводе сам базлайн — видимое действие.
- День и ночь — разные калибровки; тёмный риг читается как сломанная камера.

### 3.7 Сначала проверить прибор, потом верить измерению

- Мёртвый прибор — отказ, а не ретрай.
- Шаг A/B обязан изменить значение; «WB принял» и «наш приёмник видел» — разные факты.
- Перед диагнозом опросить всё адресное пространство: широковещательный ответ при молчании
  адресных значит «шина под питанием, гира нет». Нулевая дельта сниффера за короткое окно —
  «трафика не было», не «механизм мёртв».

Показания, которые читаются не так, как выглядят:

- Smoke сразу после цикла питания может упасть по вине стенда: дождаться первого heartbeat.
  Красный прогон сохранять сразу, иначе остаётся только число.
- `HIL-POL-10` с хоста вне LAN установки меряет канал, а не прошивку.
- Несколько гиров, вразнобой отвечающих на широковещательный запрос, чаще дают чистый `0x00`,
  чем нарушение; какие счётчики нарушений — отчёт, а какие — гейт,
  [09](../../documentation/architecture/09-dali-protocol-rules.md) §Reading answers.
- Удержания линии на десятки миллисекунд видят обе платы — это провод. Неразобранный захват
  чаще означает шину, чем потерянный тик; потери тиков считает `isr_ticks_lost` (бюджет 0).

## 4. Правила безопасности стенда

Установка — жилой свет: каждое действие на проводе видит человек. Механика — в
[README](README.md#the-installation-is-production).

1. **Видимое действие — только по отмашке** мейнтейнера на конкретный прогон: уровень,
   включение, цвет, цветовая температура. Без отмашки — `HIL_LAMPS_READ_ONLY=1`, а тесты со
   светом (колонка «Свет», §6) сняты с выборки до запуска; проверяется выборка, не исход.
   Приёмка, которой нужен свет, уходит в долг с пометкой «нужна отмашка».
2. **Всегда разрешено:** сканы Part 102/103, чтение атрибутов и банков, реестр и слайсы,
   WebSocket, поллер, репликация и арбитраж, `stats`/`diagnostics`, serial, конфигурационные
   записи с возвратом (fade time и подобные).
3. **SA01 и адреса от 4 — боевой свет:** тесты их не драйвят (`HIL_LAMP_SHORTS`), если
   отмашка не называет их явно.
4. **Сессия сохраняет установку и возвращает её**; остаток красит прогон. Что снимок
   берёт, чего не возвращает и как чинить убитую сессию —
   [README](README.md#save-and-restore).
5. **Destructive — по одному тесту на запуск**, никогда циклом (ISSUE-9). Коммишенинг
   назначает адреса и на живом сегменте не запускается.
6. **Перезагрузка контроллера — событие продакшна:** прошивка, `dut_reboot`, старт упавшего
   моста — с предупреждением мейнтейнера; что именно сбрасывает плату —
   [README](README.md#serial-ports).
7. **Стенд монопольный:** до прогона и прошивки нет чужой сессии
   (`pgrep -f "python3 -m pytest"`, чужой `esptool`) — её фон неотличим от измеряемого.

## 5. Цикл тестирования

`hil preflight` → smoke → default → slow → destructive по одному → smoke. Команды —
[README](README.md#running-tests).

| Ярус | Зачем |
| --- | --- |
| `hil preflight` | Готовность приборов, только чтение: HTTP, мост, монитор, ssh до WB, брокер, камера. |
| smoke | Приборы живы, базовые пути отвечают; до и после сессии и после любой смены стенда или прошивки. |
| default | Функциональные сьюты по §6 — всё, кроме `slow` и `destructive`. |
| slow | Соаки, поллер под чужим трафиком, латентность оператора, транзакции, банки, OTA. |
| destructive | Перезагрузки, OTA, забывание устройства; с человеком — обрыв питания и замыкание шины. По одному тесту. |

Триаж: `HIL validity` (§8) → окно сниффера теста → serial-лог → исход операции
(`hil api op <id>`).

## 6. Карта покрытия

ID — маркер `hil_id` теста ([README](README.md#writing-a-scenario)). «Свет»: **нет** —
гоняется без отмашки; **часть** — отдельные тесты меняют свет, выборку проверять по коду;
**да** — модуль про видимое действие.

| Модуль | ID | Ярус | Свет | Что доказывает |
| --- | --- | --- | --- | --- |
| `test_smoke_infra` | — | smoke | часть | Приборы живы; сниффер ловит размеренные DAPC без потерь. |
| `test_adapters` | SYS-02..04 | default | часть | Сводка и адаптеры согласованы; `enabled: false` фиксируется, тишина провода не утверждается (ISSUE-156). |
| `test_diagnostic` | DIAG-02..06 | default | часть | Сырой кадр байт в байт; `repeat_count=2`; ответ `QUERY ACTUAL LEVEL` = реестр; честное «нет ответа». |
| `test_attributes` | ATTR-01..05, 07..13 | smoke, default | часть | Чтение; fade time и его длительность; min/max; кривая; sRGB на проводе; брекет DT8. `ATTR-06` — §9.2. |
| `test_target_state` | TS-05..07 | default | да | Коалесцирование пачки, `off_all`, уровень и цвет одним PUT. |
| `test_scenarios` | — | default | часть | Образец сценария; групповой DAPC; скан без переадресации. |
| `test_optical_*` | — | smoke | да | ON/OFF, яркость, RGB, colour-only, порядок CCT. Нужна оптика. |
| `test_groups` | GRP-02..07 | default | часть | Матрица и dirty; apply программирует гир; пустой diff; конфликт. |
| `test_group_colour` | GRP-08, 09, HCL-10 | default | да | Групповой и broadcast цвет активируется на каждом члене. |
| `test_capability_gate` | CAP-01..03 | default | да | Групповой цвет режется по возможностям члена. |
| `test_scenes` | SCN-01..07 | default, slow | часть | Staged-запись; apply → `SET SCENE`; recall, групповой — одним кадром. |
| `test_dt8_scene_colour` | DT8L-01, SCNC-01 | default | да | Пределы Tc; аудит цвета сцены читает сохранённое. |
| `test_virtual_lamps` | VL-01..04 | default | часть | VL — проекция устройства; команда идёт на привязанный адрес. |
| `test_operations` | OP-01..04 | default | часть | Статусы вперёд; евикция; apply без потерь в кольце. |
| `test_fanout` | FAN-01..07 | default | да | Чужие команды проецируются в реестр с источником `sniffer`. |
| `test_hcl_runtime` | HCL-01..09 | default | да | Расписание доходит до гира в срок; override виден и снимается. |
| `test_wire_priority` | — | default | да | Оператор не ждёт чтения атрибутов (ADR-013); прерванное чтение не коммитит. |
| `test_poller` | POL-01..10 | smoke, default, slow; POL-07, 08 — destructive | часть | Настройки; источник `poller`; чужой трафик; PUT не ждёт фон (ADR-009). |
| `test_bus_load` | WIRE-01..02 | default | нет | Тики занятости провода живы и вложены; пачка запросов — наша загрузка. |
| `test_transactions` | — | slow | часть | Счётчики транзакций в гейтах; приёмная сторона §9.2 — эмулятор. |
| `test_dt6_failure` | DT6-01..04 | default | — | Отказ Part 207 по триггеру одной прелюдией и одним запросом. Эмулятор. |
| `test_memory_banks` | — | smoke, slow | нет | Банки 202–207, MASK/TMASK; банк 0 выше `0x1A`. Без гиров 51/52 — скип, и это находка. |
| `test_identify` | — | default | да | Одна пара `IDENTIFY DEVICE`, ни одной команды уровня; `powerCycleSeen` цел. |
| `test_input_devices` | INP-01..13 | smoke, default; INP-09 — destructive | часть | Скан; запись инстанса; 24-битные кадры; Part 332; событие → правило → гир; нажатие в HA. |
| `test_ws` | WS-01..06 | smoke, default | нет | Кривые кадры не роняют контроллер; слот освобождается; канал логов. |
| `test_httpd_availability` | HTTP-01 | smoke, slow | нет | httpd обслуживает сокеты во время чанковой записи; чтение без фолта стека. |
| `test_ha_bridge` | MQTT-11 | default | часть | Discovery и диалект; CCT; плитки; сцены; disable → offline; группа двигает членов. |
| `test_redundancy` | RED-01..08 | default, с пиром; RED-05 — destructive | часть | Арбитраж; отказ записи на standby; репликация; отказ и возврат; переключение. |
| `test_load` | LOAD-01..02 | slow | да | Трафик без перезагрузки; честный backpressure; доставка событий. |
| `test_persistence` | PERS-01..05 | destructive | часть | Слайсы переживают перезагрузку, runtime и операции — нет; сцены живут в гире. |
| `test_firmware_update` | OTA-01 | destructive | нет | Образ по сети загружается в другой слот; фон DALI стоит на время записи. |
| `test_pd_forget` | — | destructive | нет | Забытое устройство возвращается сканом пустым. |
| `test_commissioning` | DSC-02..04 | destructive | нет | Refresh без переадресации; цикл коммишенинга. Не запускается (§4). |
| `test_torn_slice_write` | — | destructive | нет | Обрыв питания в записи слайса — поднимается сохранённая ревизия (`HIL_POWER_CUT=1`). |
| `test_bus_short` | — | destructive | да | Замыкание шины: `bus_power_down`, `system_failure`, снятие флагов (`HIL_BUS_SHORT=1`). |
| `test_*_unit` | — | без железа | нет | Самопроверка тулкита. |

Не покрывается по построению:

- Приёмная сторона IEC 62386-101 §9.2 (окно settling кадра, разрыв последовательности у
  гира), граница 75 мс send-twice, длина стартового полубита: нужен приёмник, не делящий
  часов с передатчиком (эмулятор, арбитр) — их нет.
- Отказ Part 207, Part 251, банки 202–207, флот гиров: таких гиров и эмулятора нет, а хост и
  модель гира построены из одних таблиц и о вендорах ничего не говорят.
- Шестиканальный RGBWAF (светильников больше трёх каналов нет), xy оптически (нет оракула
  CIE-xy; дешевле селектор 208), read-back Part 103 при двух панелях (панель одна; покрыт
  хостом), два гира на одном адресе (только переадресацией).
- Переадресация на живом сегменте; формы HTTP (BDD); внешний вид web UI (глаз мейнтейнера).

## 7. Watchlist

Открытые находки стенда и приборов плюс строки-виды на открытые дефекты продукта, регрессию
которых стенд ловит сам (запись живёт в known-issues). Закрытое отсюда удаляется.

| Issue | Что наблюдаем | Как ловим | Рецепт |
| --- | --- | --- | --- |
| ISSUE-9 | Сниффер WB чувствителен к бёрстам коммишенинга: глитч-кадры нулевой длины в журнале `wb-mqtt-dali`; серийные destructive-прогоны однажды жёстко перезагрузили WB — хаб владельца и питание обеих плат. | Журнал `wb-mqtt-dali`, пропадание ssh, доступность WB в `HIL validity`. | Destructive — по одному тесту на запуск (`HIL_ALLOW_DESTRUCTIVE=1`); коммишенинг на живом сегменте не запускается. |
| ISSUE-54 | Frame server жив и один, но не забирает запросы, а клиент (и `hil preflight`) советует `--spawn-terminal`. Причина — AVFoundation не отдаёт камеру, пока устройство не перечислено; `--spawn-terminal` идемпотентен и живой сервер не трогает — совет пустой. | `hil preflight`: камера FAIL при свежем heartbeat и одном процессе сервера. | `vendor/uvc-util/uvc-util -d`, затем `hil camera-server --restart`. Заменить этим рецептом совет в `hil/camera/backends.py`. |
| ISSUE-55 | Остаток `http_transport` вне окна перезагрузки: health-проба получает отказ или сброс от работающего контроллера. Кандидат — занятая задача httpd, общий корень с ISSUE-39. | Бюджет `http_transport`; каждый повтор назван в `HIL validity`; гонки перезагрузки — отдельно (`http_reboot_race`). | Сопоставить названные события с работой httpd в тот момент. Бюджет не поднимать, пока остаток не назван. |
| ISSUE-75 | Модель гира делает случайный адрес пригодным для `COMPARE` тем же кадром `RANDOMISE`, а IEC 62386-102 даёт гиру до 100 мс: серия коммишенингов на модели зелена и на прошивке без выдержки. | Только чтением модели: прогон был бы зелёным. | Задержка готовности в модели (счётчик кадров или время вызывающего); до неё выдержку проверяет только неадресованный гир, которого на сегменте нет. |
| ISSUE-71 | Вид: пол внутренней SRAM после загрузки. | `boot_heap_budget.txt` в `HIL validity` каждого прогона. | Пол опускается только датированной строкой замера. |
| ISSUE-86 | Вид: порог захвата шины standby. | `HIL-RED-01/07/08` (`unowned`, `suppressed`, `worker_stale`), `issue86_acceptance.py`. | Приёмка — в known-issues: сутки на порогах 3 и 2, только наблюдение. |
| ISSUE-109 | Вид: лог-вызов не блокирует задачу DALI. | `console_log_unavailable` (бюджет 0), строка `log-uart` переписи. | Бюджет `log-uart` стоит `-1`: заморозить по первому полному ярусу только на чтение. |
| ISSUE-111 | Вид: перепись стеков пересоздаваемых задач. | `HIL validity`: перепись без свежего отсчёта `httpd` делает прогон невалидным. | В бюджет идут только отсчёты `fresh`. |
| ISSUE-152 | С 2026-09-23. `--fast-fade` оставляет гирам fade time 0: `_fast_fade_prep` не зависит от `production_state` и как autouse-фикстура идёт раньше по алфавиту — снимок уже с нулём, остатка нет. Справка опции и README расходятся. | `fade_time_ms` до и после сессии с `--fast-fade`. | Параметр `production_state` у фикстуры, поправить справку; до тех пор `--fast-fade` не использовать. |
| ISSUE-153 | С 2026-09-23. `identity.restore_short_addresses` шлёт `DTR0` и `SET SHORT ADDRESS` разными REST-запросами без доказательства и проверки: перезаписанный между ними `DTR0` уводит гир на чужой адрес. Из кода не вызывается. | Чтением. | `DTR0` — доказанно, как ремонт сцен в `prod_state`; проверять новый адрес и молчание старого. |
| ISSUE-154 | С 2026-09-23. Мост на WB не замечает пропажу своего порта: после переперечисления платы `ping` и `status` отвечают `ok` из кеша, `hil remote start` мост переиспользует, данных нет. | `hil preflight`: не пришла строка `HTTP access:`. | `err` от моста, когда порт пропал; перезапуск — решение оператора: старт моста сбрасывает плату. |
| ISSUE-155 | С 2026-09-23. Повторы мимо леджера: `HIL-GRP-02/06`, `HIL-DIAG-04`, `HIL-DSC-02`, `HIL-ATTR-01`, оптические тесты и `_teardown_write` молча повторяют шаг; клиент повторяет условный `PUT /rules` — после потерянного ответа это ложный `409`. | `HIL validity` их не видит. | Каждый повтор — через счётчик леджера или убрать; после транспортной ошибки `PUT /rules` сверять `GET /rules`. |
| ISSUE-156 | С 2026-09-23. Тесты не утверждают заявленного: `HIL-DSC-04` и `HIL-PERS-01` уходят в `xfail` по закрытым механизмам, `HIL-ATTR-04` — при ответе, который контракт `color_mode_override` и требует; `HIL-SYS-04` только записывает выключенный адаптер (§6 обещает «без кадров»); сверка маски групп в `test_ha_bridge` смотрит только SA0..3. | Чтением: императивный `xfail` не даёт XPASS. | Снять люки или `xfail(strict=True)` со ссылкой на запись; `ATTR-04` под контракт; `SYS-04` утверждает `adapter_disabled` и тишину провода; снять фильтр SA0..3. |
| ISSUE-157 | С 2026-09-23. `HIL validity` не видит смены образа и перезагрузки пира: идентичность переписи стеков — из манифеста `hil flash` (OTA его не пишет), аптайм пира сверяется только в ярусе `redundancy`, `serial.log` штампуется местным временем при каталогах прогонов в UTC. | Чтением. | Версия из `/api/v1/health` в идентичности, аптайм пира на границах сессии, `serial.log` в UTC. |
| ISSUE-158 | С 2026-09-23. Скрипты приёмки идут мимо тулкита: `issue86_acceptance.py` на отсутствующем логе своего чекаута проходит вхолостую, `issue89_flush_stall_ab.py` держит зашитые IP и путь и стирает имя устройства 0, `waveb_identify_probe.py` дублирует критерии `test_identify`. | Чтением. | Лог — по записи монитора, отказ на пустом окне; дубли удалить или перевести на `hil.config`. |

## 8. Критерии входа в полный прогон и формат отчёта

Прогон — приёмочный, когда:

1. `hil preflight` зелёный по приборам выбранных ярусов; без камеры — явное `--no-camera`,
   и оптика не принята.
2. Образ известен: `hil flash` (манифест: коммит, sha бинаря, ручки, сверенная версия) или OTA
   со сверкой версии вручную.
3. Есть отмашка на видимые действия с перечнем ламп; без неё прогон только на чтение и свет не
   принимает.
4. Нет незакрытого снимка установки; пир в standby (для `redundancy`); WB-мастер и шлюз живы
   (для `foreign`); чужой сессии нет.
5. Сегмент не сужен `HIL_GEAR_SHORTS`: суженный прогон `HIL validity` помечает неприёмочным.

Отчёт — `runs/<ts>/summary.md` с `HIL validity`, `manifest.json` и артефакты по тестам.
Как классифицировать красный прогон и как двигаются бюджеты —
[README](README.md#run-validity--how-a-red-run-is-classified).

Бюджеты стеков снимаются с полного default (`pytest` без `-m`): smoke не доходит до глубоких
путей. Числа сравнимы только при тех же условиях (состав реестра, двигался ли свет).

## 9. Приложение

### 9.1 Шпаргалка декода 16-битного кадра

- Адресный байт: `0AAAAAAS` — короткий адрес, `100GGGGS` — группа, `1111111S` — broadcast.
  `S=0` — DAPC, уровень во втором байте (`0xFF` — MASK: остановить фейд); `S=1` — команда.
- Спецкоманды (первый байт): `A1` TERMINATE, `A3` DTR0, `A5` INITIALISE, `A7` RANDOMISE,
  `A9` COMPARE, `AB` WITHDRAW, `AD` PING (DALI-2, кнопки и диммеры), `B1`/`B3`/`B5` SEARCHADDR
  H/M/L, `B7` PROGRAM SHORT ADDRESS, `B9` VERIFY SHORT ADDRESS, `BB` QUERY SHORT ADDRESS,
  `C1` ENABLE DEVICE TYPE, `C3` DTR1, `C5` DTR2, `C7`/`C9` WRITE MEMORY LOCATION.
- Команды (второй байт при `S=1`): `00` OFF, `05`/`06` RECALL MAX/MIN, `0A` GO TO LAST ACTIVE
  LEVEL, `10+n` GO TO SCENE, `25` IDENTIFY DEVICE, `40+n` SET SCENE, `60+n` ADD TO GROUP,
  `70+n` REMOVE FROM GROUP, `90` QUERY STATUS, `91` QUERY CONTROL GEAR PRESENT, `92` QUERY LAMP
  FAILURE, `98` QUERY CONTENT DTR0, `A0` QUERY ACTUAL LEVEL, `C0`/`C1` QUERY GROUPS, `C5` READ
  MEMORY LOCATION.
- DT8 (после `ENABLE DEVICE TYPE 8`): `E0`/`E1` SET TEMPORARY X/Y, `E2` ACTIVATE, `E7` SET
  TEMPORARY COLOUR TEMPERATURE, `F3`/`F7` STORE/QUERY GEAR FEATURES/STATUS, `FA` QUERY COLOUR
  VALUE.
- Backward — 8 бит, значение ответа; 24-битные кадры — Part 103, своя кодировка.

### 9.2 Долг стенда: подтверждения, которые никто не должен выводить заново

Подтверждения, которые получаются только на установке. Приёмки исправлений, ждущих стенда, —
в known-issues. Пункт уходит, когда закрыт.

- **Ремонт бита Automatic Activation (DT8, команда 243) ни разу не срабатывал на реальном
  светильнике.** Бит — RAM, после включения питания установлен, а ремонт запускается только
  по сохранённому наблюдению «сброшен». Последовательность исполнителя (доказанный `DTR0`,
  243 дважды, сверка по 247 — одной транзакцией) доказана только хост-симом; сырой путь —
  другой носитель: отдельные HTTP-вызовы не образуют пару send-twice. Рецепт (`HIL-ATTR-06`):
  сбросить бит на одной лампе через `/api/v1/dali/raw`, прочитать атрибуты `dt8_color`
  (реестр видит `0x00`, ремонт разрешён в `/api/v1/settings/dali`), сделать цветовую запись,
  проверить 247 = `0x01` и активацию селектором 208. Нужна отмашка.
- **ID формата содержимого банка 1 (`0x11`/`0x12`) на установленных светильниках не
  прочитан.** От него зависит, Part 251 ли выше `0x10` (`3`, `4`, `5`) или байты
  производителя, которые не разбираются.
- **Путь `several_failures` пробника здоровья шины на железе не доказан:** нужны два гира с
  отказом лампы, вразнобой отвечающих на широковещательный `QUERY LAMP FAILURE`
  (IEC 62386-101 §8.2.5), а без поломки светильника этого не вызвать. На хосте путь доказан
  только моделью гира.
- **Приёмка `HIL-FAN-*` ждёт исправного шлюза WB-MDALI:** пока шлюз срывает собственные
  передачи независимо от нашего трафика, красный FAN-тест — о приборе. Лечение — питание
  WB-MDALI или перезагрузка WB (перезапускает обе платы) — решает мейнтейнер; FAN меняет свет.
- **Кто держит линию доминантой десятки миллисекунд, не установлено.** Обе платы видят
  удержания одновременно, значит это провод; кандидаты — WB, светильник на пусковом токе,
  питание шины. Сопоставлять строки `DALI line held` с событиями питания и опросом WB.
- **`dali_worker` не перебазирован правилом «прежняя глубина плюс кадр прерывания»** и на
  ярусах со светом выходит за `stack_budget.txt`: перебазировать с причиной или найти
  глубину — не решено.
